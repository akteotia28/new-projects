name: My first Github action file
on:
  push:
    branches:
    
      - main
      - 'feature/**'

jobs:
  my_first_job:
    name: My first job
    runs-on: self-hosted
    steps:
      
      - name: Wellcome CMD
        run: echo Hello from CMD
        shell: cmd





        
          
  
